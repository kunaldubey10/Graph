# Herb Workspace

Main application lives in `HerbalTrace/`.

For a fresh laptop setup and exact run commands, start here:

- `HerbalTrace/README.md` (see **Run On A Different Laptop (Windows, Fresh Setup)**)
